---
interface Props {
    href: string;
    target?: "_self" | "_blank" | undefined;
}

const {href, target = "_blank"} = Astro.props as Props;
---

<a href={href} target={target} rel={target === "_blank" ? "noopener noreferrer" : undefined}
    ><slot /></a
>
